os: linux
dist: xenial
language: node_js
node_js:
  - 10
services:
  - xvfb
addons:
    chrome: stable
before_install:
    - export CHROME_BIN=chromium-browser
    - # "export DISPLAY=:99.0"
    - # "sh -e /etc/init.d/xvfb start"
    - # sleep 3 
    - # google-chrome-stable --headless --disable-gpu --nosandbox --remote-debugging-port=9222 http://localhost &